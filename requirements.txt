Pillow>=10.0.0
PyYAML>=6.0
requests>=2.31.0
deep-translator>=1.11.4
python-dotenv>=1.0.0
opencv-python-headless>=4.8.0
Flask>=3.0.0
psd-tools>=1.9.0
better-profanity>=0.7.0
# Optional: the no-text check on generated backdrops. Needs the Tesseract
# binary too (macOS: brew install tesseract). Without either, the check
# reports itself as unavailable and the run carries on.
pytesseract>=0.3.10
# Vector shape rendering for psd-tools. A template whose CTA is a group
# containing a rectangle has a vector shape layer in it, and without this
# psd-tools raises ImportError from composite() the moment any layer's
# visibility is toggled -- which is how the app isolates layers. Missing,
# every per-layer operation silently returns nothing and the render loses
# the layers it was meant to keep.
aggdraw>=1.3.15
# Layer-effect rendering for psd-tools. Needed to rebuild a PSD's stored
# flattened preview from its layers after this app edits them (the "save
# this copy into the templates" option): without it composite(force=True)
# raises ImportError on any document carrying an effect or a vector
# stroke, the refresh silently fails, and the next run draws the new copy
# on top of the old placeholder still sitting in the stale preview.
scikit-image>=0.21
# Background removal for the whole-ad layer split (src/ad_split.py):
# the model's flat picture is pulled apart into background / subject /
# painted-text layers, and this is what cuts the subject out cleanly.
# Optional -- without it OpenCV's GrabCut is used, which is rough at
# hair and edges. Downloads its model (176 MB, once) on first use.
rembg[cpu]>=2.0.60
# ffmpeg for the motion versions (src/motion.py): a looping MP4 per size,
# made from the size's own layers. Bundles a static ffmpeg binary, so
# nothing else to install.
imageio-ffmpeg>=0.5
# Scene-text detector for the no-text check on generated backdrops (models bundled in the wheel).
rapidocr-onnxruntime>=1.3
# iPhone HEIC photos on the mood board (converted to PNG on the way in).
pillow-heif>=0.16
