Pillow>=10.0.0
PyYAML>=6.0
requests>=2.31.0
deep-translator>=1.11.4
python-dotenv>=1.0.0
opencv-python-headless>=4.8.0
Flask>=3.0.0
psd-tools>=1.9.0
better-profanity>=0.7.0
# Optional: the no-text check on generated backdrops. Needs the Tesseract
# binary too (macOS: brew install tesseract). Without either, the check
# reports itself as unavailable and the run carries on.
pytesseract>=0.3.10
# Vector shape rendering for psd-tools. A template whose CTA is a group
# containing a rectangle has a vector shape layer in it, and without this
# psd-tools raises ImportError from composite() the moment any layer's
# visibility is toggled -- which is how the app isolates layers. Missing,
# every per-layer operation silently returns nothing and the render loses
# the layers it was meant to keep.
aggdraw>=1.3.15
